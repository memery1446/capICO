import { useState } from 'react';

import { ethers } from 'ethers'

const Buy = ({ provider, price, capico, setIsLoading }) => {

	return (
		<Form>
			<Form.Group>

			</Form.Group>
		</Form>

		)

}